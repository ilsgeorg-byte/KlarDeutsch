# 🚀 KlarDeutsch - Первый запуск

## Шаг 1: Проверка системы

Запустите скрипт проверки конфигурации:

```bash
# Windows:
python check_setup.py

# Linux/Mac:
python3 check_setup.py
```

Этот скрипт проверит все необходимые компоненты.

---

## Шаг 2: Подготовка окружения

### Способ 1: Автоматический запуск (рекомендуется)

```bash
# Windows:
start.bat

# Linux/Mac:
chmod +x start.sh
./start.sh
```

Это установит все и покажет команды для запуска.

### Способ 2: Ручная установка

#### 2a. Клонируйте .env.local

```bash
cp .env.local.example .env.local
```

Отредактируйте `.env.local` и установите правильный `POSTGRES_URL`:

```ini
# Локальный PostgreSQL (по умолчанию):
POSTGRES_URL=postgresql://postgres:password@localhost:5432/klardeutsch

# Или используйте ваш URL
```

#### 2b. Установите зависимости фронтенда

```bash
npm install
```

#### 2c. Установите зависимости бэкенда

```bash
# Windows:
python -m venv api\venv
api\venv\Scripts\activate.bat
pip install -r requirements.txt

# Linux/Mac:
python3 -m venv api/venv
source api/venv/bin/activate
pip install -r requirements.txt
```

#### 2d. Инициализируйте БД

```bash
cd api

# Создайте таблицы:
python db.py

# Добавьте слова:
python seed.py

cd ..
```

---

## Шаг 3: Запуск серверов

Откройте **два разных терминала**:

### Терминал 1: Flask сервер

```bash
cd api
python app.py
```

Вы должны увидеть:
```
 * Running on http://127.0.0.1:5000
 * Debug mode: on
```

### Терминал 2: Next.js сервер

```bash
npm run dev
```

Вы должны увидеть:
```
 ▲ Next.js 14.1.0
  Local:        http://localhost:3000
```

---

## Шаг 4: Откройте приложение

Перейдите в браузер: **http://localhost:3000**

Вы должны увидеть главную страницу KlarDeutsch.

---

## 🧪 Проверка работоспособности

### Проверка API (используя curl)

```bash
# Получить список слов
curl http://127.0.0.1:5000/api/words?level=A1

# Получить уровни
curl http://127.0.0.1:5000/api/levels

# Проверить здоровье сервера
curl http://127.0.0.1:5000/health
```

### Проверка в браузере

1. Откройте **http://localhost:3000**
2. Нажмите "Начать тренировку слов →"
3. Выберите уровень A1
4. Должны появиться немецкие слова
5. Нажмите на значок микрофона чтобы записать аудио
6. Перейдите в "Записи" и послушайте

---

## ⚠️ Распространённые ошибки

### "ModuleNotFoundError: No module named 'routes'"

**Решение:** Убедитесь что в папке `api/routes/` есть файл `__init__.py`

```bash
# Проверьте:
ls api/routes/__init__.py

# Если нет, создайте пустой файл:
touch api/routes/__init__.py
```

### "psycopg2.OperationalError: connection failed"

**Причины:**
1. PostgreSQL не запущен
2. Неверный POSTGRES_URL в .env.local
3. БД не существует

**Решение:**
```bash
# Создайте БД вручную:
createdb -U postgres klardeutsch

# Или проверьте что PostgreSQL запущен:
sudo systemctl start postgresql  # Linux
brew services start postgresql   # Mac
# Windows: запустите PostgreSQL Server из Services
```

### "CORS error при загрузке аудио"

**Причина:** Flask может быть на другом порту или не запущен

**Решение:**
```bash
# Убедитесь что Flask запущен:
curl http://127.0.0.1:5000/health

# Должен вернуть: {"status":"ok"}
```

### "Модуль не найден: next.config.mjs"

**Решение:** Очистите Next.js кеш
```bash
rm -rf .next
npm run build
npm run dev
```

---

## 📊 Проверка структуры проекта

Ваша папка должна выглядеть так:

```
KlarDeutsch/
├── api/
│   ├── routes/
│   │   ├── __init__.py         ✓ Должен существовать
│   │   ├── words.py             ✓ Должен существовать
│   │   └── audio.py             ✓ Должен существовать
│   ├── data_words.py            ✓ Должен существовать
│   ├── db.py
│   ├── index.py
│   ├── seed.py
│   ├── app.py
│   └── uploads/                 ✓ Папка (может быть пустая)
│
├── app/
│   ├── components/
│   │   └── ErrorBoundary.tsx    ✓ Должен существовать
│   ├── styles/
│   │   └── Shared.module.css    ✓ Должен существовать
│   ├── trainer/
│   │   ├── page.tsx
│   │   └── Trainer.module.css   (можно удалить - копия в styles/)
│   ├── audio/
│   │   └── page.tsx
│   ├── layout.tsx
│   ├── page.tsx
│   └── globals.css
│
├── .env.local                   ✓ Должен быть (из .env.local.example)
├── .env.local.example
├── .gitignore
├── requirements.txt
├── next.config.mjs
├── README_IMPROVEMENTS.md
├── CHANGES_SUMMARY.md
├── check_setup.py
├── FIRST_RUN.md                 ← Вы здесь
├── start.bat
├── start.sh
├── package.json
├── tsconfig.json
├── postcss.config.js
└── tailwind.config.js
```

---

## ✅ После успешного запуска

1. **Сделайте commit:**
   ```bash
   git add .
   git commit -m "Refactor: restructure and improve project"
   git push origin main
   ```

2. **Читайте документацию:**
   - `README_IMPROVEMENTS.md` - подробное руководство
   - `CHANGES_SUMMARY.md` - что изменилось
   - `check_setup.py` - для проверки конфигурации

3. **Тестируйте функции:**
   - Переключение уровней
   - Воспроизведение звука
   - Запись произношения
   - Список записей

4. **Планируйте следующие улучшения:**
   - Сохранение прогресса пользователя
   - Аутентификация
   - Проверка произношения
   - Mobile версия

---

## 🆘 Нужна помощь?

### Если не работает полностью:

1. Запустите `python check_setup.py` для диагностики
2. Проверьте логи в терминалах
3. Read `README_IMPROVEMENTS.md`

### Если есть конкретная ошибка:

1. Скопируйте полный текст ошибки
2. Поищите в Google первую строку ошибки
3. Проверьте раздел "Распространённые ошибки" выше

---

Удачи! 🎉

Если всё работает корректно - можете начинать добавлять больше функционала!
